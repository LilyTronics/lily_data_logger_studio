name: Unit tests on Ubuntu

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to test'
        required: true
        default: 'staging'

jobs:
  test:
    runs-on: ubuntu-latest
  
    steps:
      - name: Install required packages
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev libglib2.0-dev libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev \
            libwebkit2gtk-4.0-dev libgl1-mesa-dev libegl1-mesa-dev libcurl4-openssl-dev
        
      - name: Check out repository
        uses: actions/checkout@v6
    
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
    
      - name: Install dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Set PYTHONPATH to repo root
        shell: bash
        run: |
          echo "PYTHONPATH=$GITHUB_WORKSPACE" >> $GITHUB_ENV

      - name: Run tests
        shell: bash
        run: |
          python tests/run_all_unit_tests.py
      
      - name: Upload test report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: TestRunnerReport-${{ github.run_id }}
          path: tests/test_reports/
          retention-days: 30
          
