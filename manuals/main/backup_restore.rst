Backup and restore
------------------

You can backup and restore your data.

Backup:

* Save the configuration to a file.
* Export all test runs to SQLite (recommended) or JSON.

We recommend SQLite because the file is more compact.

Restore:

* Load the saved configuration.
* Import the test runs in the test run data manager.

Everything should be restored.
